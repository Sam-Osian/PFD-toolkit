{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "uv run gunicorn --chdir django_workbench pfd_workbench.wsgi:application --bind 0.0.0.0:$PORT --timeout 420 --graceful-timeout 60"
  }
}
